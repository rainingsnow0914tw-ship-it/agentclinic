"""AgentClinic core — deterministic, evidence-bound forensic analysis of AI
agent execution traces.

Chain: Trace Evidence Contract -> Deterministic Score -> Evidence-backed
Finding -> (P2) Test Cloud Execution Record.

Roles, never mixed: rule engine = judge, LLM = coach (P4, bounded),
Test Cloud = record (P2), UiPath = orchestrator (P2)."""

__version__ = "0.1.0"
